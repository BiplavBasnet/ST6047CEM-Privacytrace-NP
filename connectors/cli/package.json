{
  "name": "privacytrace-connect",
  "version": "0.1.0",
  "private": true,
  "description": "Installer CLI for PrivacyTrace Runtime, Wazuh, and GitHub Actions connectors. Not published to the public npm registry.",
  "bin": {
    "privacytrace-connect": "./bin/privacytrace-connect.js"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "bin",
    "src",
    "README.md"
  ],
  "scripts": {
    "test": "node --test tests/*.js",
    "pack:inspect": "npm pack --dry-run"
  }
}
