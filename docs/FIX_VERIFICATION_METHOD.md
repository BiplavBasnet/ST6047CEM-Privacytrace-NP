# Fix Verification Method

Fix verification compares retest evidence against the original exposure profile (service/endpoint/sensitive type/location). Outcomes persist as `FixVerification` and, when the verified remediation path runs, `VerificationOutcome`.

Human review remains required before treating a pass as authoritative closure.
