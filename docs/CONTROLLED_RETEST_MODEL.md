# Controlled Retest Model

Retests use FIXED_LOG / FIXED_SCAN evidence or live-monitor retest helpers. Matching is problem-specific (same service/endpoint/exposure profile) — not a generic “run all tests again”. Results feed verification outcomes; they do not auto-close incidents.
