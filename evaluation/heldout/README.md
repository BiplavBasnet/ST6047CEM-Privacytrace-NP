# Held-out evaluation pack (not application freeze)

Inputs and ground truth are separated. PrivacyTrace runtime must not import `ground_truth.yaml`.
`backend/app/evaluation_data/` remains DEVELOPMENT/PRELIMINARY.

Wazuh / GitHub hosted workflow: NOT AVAILABLE this run (HO-075–078 excluded from denominators).
